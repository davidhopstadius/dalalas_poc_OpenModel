# Secrets
.env
.env.*
!.env.example

# Kalldokument for indexering (manualer ska inte in i repot)
documents/*
!documents/.gitkeep

# RAG-index (genererade embeddings/chunks)
rag_index/

# Slask-korningar av eval (namngivna baslinjer som steg1.json committas)
eval/results/latest.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
